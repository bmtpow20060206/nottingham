CHIP statusBits{
	IN temp[16];
	
	OUT zg, ng;

	PARTS:
	//Status bits
	And(a = temp[15], b = true, out = ng);

	//idk what im doing
	Or8Way(in = temp[0..7], out = zg1); //part of zg not actual zg sorry i couldn't think of other variable name
	Or8Way(in = temp[8..15], out = zg2);
	Or(a = zg1, b = zg2, out = notZg);
	Not(in = notZg, out = zg); 
}